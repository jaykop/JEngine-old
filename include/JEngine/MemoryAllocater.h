#pragma once
#include "Macro.h"

JE_BEGIN

// TODO
class MemoryAllocater {

public:

private:

	MemoryAllocater() = delete;
	~MemoryAllocater() = delete;
	MemoryAllocater(const MemoryAllocater& /*_copy*/) = delete;
	void operator= (const MemoryAllocater& /*_copy*/) = delete;

};

JE_END